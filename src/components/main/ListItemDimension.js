import React from "react";

function ListItemDimension({ primary, secondary }) {
  return (
    <div>
      <h4>{primary}</h4>
      <p>{secondary}</p>
    </div>
  );
}

export default ListItemDimension;
